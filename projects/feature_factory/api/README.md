# API Service

## Purpose
Accept batch feature requests, schedule runs, and return status/results.

## Planned Endpoints
- `POST /batches` create a batch
- `GET /batches/{id}` batch status
- `GET /features/{id}` feature details

## Entry Point
`projects/feature_factory/api/app.py` (placeholder)
